# 2026-07-29, the ARC signing-half work (audit 11.28). All three growths are the
#   RFC 8617 5.1.2 and 5.1.3 fixes plus their reasoning, and seal_cli.zig at 325 is a
#   new conformance tool that is already under the goal:
#     arc.zig   528 -> 552  chainAlreadyBroken, and why "already recorded as broken"
#                           is a narrower question than "fails validation now" --
#                           getting that backwards would stop us marking newly
#                           detected failures, which is the worse defect.
#     flow.zig  419 -> 433  the 5.1.3 refusal to continue a broken chain.
#     main.zig  603 -> 617  msgfile wired in, plus the note that an unreferenced
#                           @import is never analyzed, so its tests silently do not
#                           run -- which is how msgfile's three tests went missing.
# Files currently over the 400-line goal, with the ceiling each
# may not exceed. Ceilings tighten by themselves as files shrink; a
# file that reaches the goal drops off this list entirely.
#
# Adding an entry by hand is how you knowingly accept a long file:
#   zig build lint -- --update
# An empty list below means every file meets the goal.
552 src/arc.zig
891 src/chain.zig
433 src/flow.zig
617 src/main.zig
420 src/sealbuild.zig
