# 2026-07-29, the ARC signing-half work (audit 11.29). All three growths are the
#   RFC 8617 5.1.2 and 5.1.3 fixes plus their reasoning, and seal_cli.zig at 325 is a
#   new conformance tool that is already under the goal:
#     arc.zig   528 -> 552  chainAlreadyBroken, and why "already recorded as broken"
#                           is a narrower question than "fails validation now" --
#                           getting that backwards would stop us marking newly
#                           detected failures, which is the worse defect.
#     flow.zig  419 -> 433  the 5.1.3 refusal to continue a broken chain.
#     main.zig  603 -> 617  msgfile wired in, plus the note that an unreferenced
#                           @import is never analyzed, so its tests silently do not
#                           run -- which is how msgfile's three tests went missing.
#
#   THIS BLOCK WAS DELETED ONCE BY THE LINT TOOL ITSELF and recovered from git. The
#   allowlist writer regenerated the file from scratch, discarding every comment it
#   had not emitted -- on the ordinary self-tightening path, not just --update. Fixed
#   in securemilter-lib/tools/check-line-limit.sh: comments above the generated
#   header are now carried across a rewrite.
#
# 2026-07-29, A-1 CLOSED: main.zig 617 -> 615, src/seal.zig deleted outright.
#   It held a placeholder AMS signing-input builder that signed the synthetic string
#   "ams:{i}:{d}:{h}:{bh}" instead of canonicalized headers, so any seal made through
#   it failed every compliant verifier including securearc itself. Dormant --
#   constructSeal had no callers -- but a loaded trap for the next refactor.
#
#   Deleted rather than repaired because A-1's other option ("move the proven
#   builders into it and use them") had already happened under a different name:
#   sealbuild.zig holds them, production and securearc-seal both call it, and it is
#   verified 17/17 against an external signer. Keeping seal.zig bought nothing.
#
#   Its two tests asserted a struct literal holds the values just assigned to it, and
#   that dupe-then-free does not crash. Neither touched the placeholder, so the file
#   read as covered while its Critical defect was untested. Test total 67 -> 65.
#
# 2026-07-29, refactor plan stage 3.2: flow.zig 433 -> 501, A CEILING RAISED ON
#   PURPOSE, which is the one thing this file exists to make deliberate.
#
#   doSeal was 136 lines at NESTING DEPTH 6 -- the deepest function in the repository.
#   48 of those lines were a single labelled `blk:` block containing a `switch` inside
#   a `switch`, whose whole job was to decide one value: the `cv=` of the set we are
#   about to add. One question, four answers, six levels of indentation.
#
#   Now `ChainOutcome` names the four answers, `chainOutcome` decides, and
#   `dnsTempOutcome` applies the operator's On-DNSError policy. Measured: doSeal 136 ->
#   95 lines and depth 6 -> 4; the file's maximum depth 6 -> 4.
#
#   THE FILE GOT LONGER AND THAT IS THE CORRECT TRADE. A union type with its variants
#   documented, plus two function signatures, is more lines than the block it replaced
#   and fewer things to hold in your head at once. Length is the symptom this gate can
#   measure; depth is what actually made doSeal hard to read.
#
#   OF THE 68 LINES, 14 ARE THE COMMENT INTRODUCING THE NEW TESTS, which sits outside
#   the `test` blocks and therefore counts as production -- the known limitation
#   recorded in 11.31. The extraction itself is +54. Noted because the first draft of
#   this note said 487, measured before those tests existed, and a ceiling rationale
#   whose numbers do not match the ceiling is worse than none.
#
#   Two hazards found while doing it, both recorded in the code rather than here:
#     - `stamp_temperror` is a variant rather than a write inside the decision, so
#       every write to the message stays in doSeal. That keeps X-8's rule structural.
#     - the reported instance is CARRIED, not recomputed. An earlier draft substituted
#       sets[sets.len-1].instance for vr.highest_instance; chain.zig sets that field to
#       the loop index, the offending set's instance, the newest set's instance, or a
#       literal 1 or 0 depending on where it gave up, so the two are not the same
#       number whenever a chain broke anywhere but its newest set. Caught by reading
#       chain.zig -- no case covered it, which is why FOUR NOW DO. Test total 65 -> 69,
#       and the skip-seal one was teeth-checked by reintroducing the substitution:
#       "expected 3, found 0".
#
#   The tests exist because the extraction made `dnsTempOutcome` pure -- policy and a
#   ValidationResult in, a decision out, no DNS and no daemon. Before it, the same
#   three branches were only reachable by standing up a resolver that fails
#   transiently, which is why they had never been tested.
#
# 2026-07-29, refactor plan stage 3.3: main.zig 581 -> 597, also deliberate.
#
#   reloadConfig ended in FOUR NESTED LEVELS of `if (x) |y| ... else |err|` to reload
#   the seal key, which put the success case -- a single log.info -- at the deepest
#   point in the function, and put each failure's recovery in an `else` branch further
#   from its cause than the code it was recovering from. Now `reloadSealKey` with three
#   guards, each with its cleanup beside it. reloadConfig 72 -> ~50 lines and off the
#   depth>=4 list entirely; the file's maximum depth 6 -> 5, the remainder being main()
#   itself, which is stage 2's job.
#
#   The ownership contract is subtle and is now stated where it applies rather than
#   implied by nesting: boxSealKey only copies into heap storage, so on ITS failure the
#   key is still the caller's to deinit, while after a successful box the cleanup path
#   is freeSealKey. Getting that backwards leaks a key or double-frees one, and the old
#   shape expressed it only through which `else` branch you were standing in.
#
#   +16 net: a signature, its doc, and two ownership comments, against 21 lines of
#   pyramid removed. EXPECT THIS CEILING TO FALL SHARPLY at stage 4, when the config
#   parser and the flow move out of main.zig the way securedkim's will; raising it by
#   16 now is not an argument that 597 is a reasonable size for this file.
#
#   Also corrected reloadConfig's own doc comment, which still said "re-reads seal key"
#   -- accurate when written, and the entirety of what A-10/A-14 was about.
#
# 2026-07-30, X-16(b): main.zig +6, RAISED DELIBERATELY. Same change in all four daemons.
#
#   Every fatal startup failure used to be invisible. `daemonize` points stderr at
#   /dev/null, so the error Zig's start code prints for an error returned from `main`
#   goes to a descriptor nobody reads, and syslog -- the only channel still open after
#   the fork -- never received it. Measured: a daemon told to bind an unusable address
#   exited 0, left nothing listening, and logged "starting" as its last word.
#
#   The fix is the SHAPE of `main`, not a line inside it. The body moved to `runDaemon`
#   and `main` became one line:
#
#       runDaemon() catch |e| return bootstrap_mod.fatal(e);
#
#   so every error converges on one report -- including from a `try` added later.
#   Per-site catch blocks were the alternative, and are how X-7 came to be fixed in one
#   of four copies.
#
#   +6 is the entire cost: three lines of `main`, one blank, and a two-line comment
#   pointing at `bootstrap.fatal` where the reasoning is written once. It was +10 until
#   the gate objected to the same six-line rationale appearing in four files, which was
#   a fair objection and is why it now lives in the library.
#
#   NOT PAID DOWN HERE ON PURPOSE. These four main.zig files are already the roadmap's
#   next extraction target; +6 against a planned several-hundred-line reduction is
#   noise, and this ceiling should be overtaken by that work rather than by a separate
#   tidy-up.

# 2026-07-30, X-16(a): main.zig +3, RAISED DELIBERATELY. Same change in all four daemons.
#
#   The readiness handshake. `daemonize` now returns the write end of a pipe and the
#   ORIGINAL process blocks on the read end instead of exiting 0 immediately, so
#   `service start` finally reports what happened. Measured before: an unbindable
#   listener gave parent exit 0, nothing listening, no process, and rc.d announcing a
#   started service. After: exit 1.
#
#   The daemon has to say when it is actually serving, and only the daemon knows --
#   everything that makes it useful (binding, the worker pool) happens AFTER
#   bootstrap.run returns. So one line lands here, after the pool spawns:
#
#       boot.notifyReady();
#
#   +3 is comment, call, blank. The reasoning is in Bootstrap.notifyReady's doc
#   comment, not repeated here -- the first attempt carried a four-line rationale in
#   all four daemons and the gate objected, correctly, for the second time in one day.
#
#   NOT PAID DOWN HERE ON PURPOSE, same as the X-16(b) note above: these files are the
#   roadmap's settings.zig extraction target and should be cut there, not nibbled here.

# 2026-07-31, D-23: chain.zig 604 -> 606, main.zig 623 -> 626, sealbuild.zig 408 -> 412.
#
#   +9 across three files so an inbound AMS can be validated under the `c=` its
#   sender chose. A milter is handed a header name and value, never the field's
#   original octets, so the separator was fabricated as `name + ": " + value`;
#   `c=simple` hashes the field verbatim, and an AMS whose field carried no space
#   after the colon could not verify. See `securemilter-lib` `header.zig` for the
#   measurement and the rule.
#
#   Where it went: `arc.Header.had_space` and `ArcSet.ams_had_space` (the AMS is
#   part of its own signing input, so its own separator is hashed too), plus the
#   `protocol_flags` entry and short comments. `sealbuild.zig` is the odd one --
#   its four lines buy nothing today, because that path is hardcoded `.relaxed`
#   and relaxed deletes whitespace around the colon either way. It renders through
#   the shared helper regardless, so a future `c=` reaching that path does not
#   silently inherit the bug, and the comment says exactly that.
#
# 2026-07-31, A-13: chain.zig 606 -> 628, +22.
#
#   RFC 8617 4.1.3 says the DKIM `h` tag on an ARC-Seal "is NOT allowed and, if found,
#   MUST result in a cv status of fail". Nothing read `h=` on an AS, so the MUST was
#   unimplemented -- the verdict came out `fail` anyway, but only because the fixed
#   5.1.1 scope was used regardless and the hash therefore disagreed.
#
#   Seven lines of code: `sealCarriesH` beside `signsArcSeal`, and the branch in
#   `verifySeal` ahead of the DNS lookup. The rest is the note explaining why an
#   accidentally-correct verdict was still worth fixing -- the accident points the
#   wrong way, since honouring `h=` on a seal would let a sealer choose which ARC
#   headers its own seal covers.
#
#   THE VALIMAIL SUITE DOES NOT COVER THIS, which is why the unit test does. Its
#   `as_fields_h_present` case looks like it should: the mock resolver NXDOMAINs
#   test._domainkey.example.com, so the AMS fails at its key lookup and `verifySeal` is
#   never reached. Established by making the new branch return `internal_error` (which
#   maps to cv=unknown, not cv=fail) and watching the suite still score 171/171.
#
# 2026-08-04, securearc: three ceilings recorded, nothing split. Reasoning per
# file below, and the growth is measured from the history in each case rather
# than assumed. This repo is already the most-divided in the suite --
# settings.zig, flow.zig, sealbuild.zig and msgfile.zig have all been cut out of
# main.zig -- so what remains is not undivided bulk.
#
# main.zig 630, ceiling 626. FOUR LINES OVER.
#
#   589 after the pool move, then 623 at `5f65b5f` (X-3, MsgCtx carrying the
#   worker's resolver instead of building one per message), 626 at `04d1e2a`
#   (D-23) and `98cd5bd` (L-2), and 630 at `51c7330` -- logging the client
#   address the protocol gave us rather than only the macro. That last commit is
#   the whole of this breach.
#
#   THE SEAM IS REAL AND IS NOT BEING TAKEN TODAY, on grounds of proportion.
#   Eight functions here are one concern -- `freeReloadable`, `adoptReloadable`,
#   `snapshot`, `freeSealKey`, `boxSealKey`, `reloadSealKey`, `reloadConfig`,
#   `onWorkerReload`, plus the two `Rcu` cells they publish through -- roughly
#   150 lines of live-configuration swapping. Extracting it would take this file
#   to about 484.
#
#   Against that: this is the most lifetime-critical code in the daemon. X-2
#   (a seal key replaced between the two signatures of one ARC set), X-11
#   (ownership of a value whose publication failed) and A-14 (a message running
#   against a half-swapped configuration) were all fixed *in these functions*,
#   and `onWorkerReload` reaches back into `main.zig`'s threadlocal resolver, so
#   the move is not the one-way dependency the `cache.zig` split was. Trading a
#   use-after-free risk in reload for four lines is a bad trade, and "it is only
#   a file move" is exactly what would make such a bug easy to introduce.
#
#   THE NUMBER TO HIT WHEN IT IS TAKEN: 484, with the RCU cells and all eight
#   functions moving together -- they share `g_reloadable`, `g_seal_key`,
#   `g_config_gen`, `g_config_path` and `g_allocator`, so a partial move would
#   leave the globals split across two files, which is worse than either end
#   state. Best done when reload is being touched for its own sake.
#
# flow.zig 526, ceiling 491. THIRTY-FIVE LINES, ALL ARC ORDERING CORRECTNESS.
#
#   491 through D-23 and X-3, then three commits that are each about the bytes
#   that actually go on the wire: 506 at `575bd9c` (emit the ARC set seal-first,
#   because `addHeader` appends), 517 at `5000e83` (space the A-R and keep the
#   sealed set byte-exact), 526 at `5d168c7` (seal what is transmitted --
#   `emitArcSet` takes the negotiated flag).
#
#   RFC 8617 requires the seal to cover the set as transmitted, so the ordering,
#   the spacing and the negotiated-flag handling are one rule with one failure
#   mode: a chain that validates here and breaks at the next hop. Splitting the
#   emit path from the seal path would put that rule in two files. The ValiMail
#   suite passes 171/171 and is what holds this honest.
#
# arc.zig 420, twenty lines over the goal and no previous ceiling.
#
#   246 -> 285 (A-4/A-9) -> 317 -> 341 (ValiMail signing) -> 364 (D-23) -> 420
#   at `c02655b`, parsing the instance production properly instead of
#   approximating it (A-11). That commit is +56 and it is the one that put the
#   file over.
#
#   It is one header parser: `parseArcSets` at ~125 lines with
#   `parseInstanceProduction`, `parseAmsTags`, `parseSealTags`, `stripFws` and
#   `findTag` beneath it, plus the small types they fill in. A-11 is precisely a
#   case where approximating the grammar was the bug, so the production is
#   spelled out; that is the length being bought. Cutting the helpers away from
#   the parser that is their only caller would gain a file and lose the ability
#   to read the grammar in one place.
#
#   Verified unchanged while recording these: unit 82/82, ValiMail 171/171.

# 2026-08-04, LATER THE SAME DAY: main.zig 630 -> 647, the safe-key check.
#
#   SAID PLAINLY BECAUSE THE ENTRY ABOVE IS HOURS OLD. That note recorded 630,
#   observed the file had grown four lines past 626, and named an extraction
#   worth ~150 lines with a target of 484. This is the second growth since, and a
#   ceiling that moves twice in a day is the pattern the note itself called out
#   for `worker.zig` -- where a 121-line paydown was 94 lines spent. Recording it
#   in the same terms rather than absorbing it quietly.
#
#   The seventeen lines are the two branches that report a refused seal key: one
#   at startup where the load is fatal, one in `reloadSealKey` where it is not and
#   the previous key stays live. Both were cut to a single `log` call each by
#   moving the wording into `crypto.KEY_PERMISSIONS_ADVICE`, which is shared with
#   securedkim so the three daemons cannot describe the same fault three ways.
#   What remains is the branch itself, and it has to be a branch: the generic
#   "failed to load" line sends an operator looking for a corrupt PEM, when the
#   fault is a mode and the fix is one `chmod`.
#
#   The extraction target is unchanged at 484 and none of this lands in the RCU
#   reload block that the target covers -- so the case for taking it is now
#   stronger by seventeen lines, and no harder. If this file grows a third time
#   before that extraction happens, the extraction should stop waiting for reload
#   to be touched for its own sake and simply be scheduled.
#
# main.zig 647 -> 648. ONE LINE: `.umask = arc_cfg.umask,` in the bootstrap call.
#
#   The socket-permission work (UMask, and User accepting user:group). The daemon
#   side of it is a single field forwarded to `bootstrap.run`; everything with any
#   substance to it went into securemilter-lib's new `credentials.zig`, and the
#   operator-facing explanation went into the man page and the conf sample.
#
#   THIS IS THE THIRD GROWTH, WHICH THE 647 ENTRY BELOW SAID WOULD BE THE TRIGGER:
#   "if this file grows a third time before that extraction happens, the extraction
#   should stop waiting for reload to be touched for its own sake and simply be
#   scheduled." So it is scheduled, and deliberately NOT taken in this commit.
#
#   Why not here. The change that pushed it over is one line of config forwarding
#   in a security fix; the extraction is ~150 lines of the most lifetime-critical
#   code in the daemon, the code where X-2, X-11 and A-14 were each fixed. Landing
#   both together produces a diff in which a use-after-free in ARC sealing could
#   hide behind a socket-permissions change, and neither half can be reviewed on
#   its own terms. That is a worse outcome than a file one line longer.
#
#   WHAT WAS LEARNED THIS TIME, which the earlier entries did not record: the
#   eight functions are not self-contained even within main.zig. `g_seal_key` is
#   read on the MESSAGE PATH (`.sign_key = g_seal_key.get()`), initialised during
#   startup, and torn down at shutdown -- so the extraction is not a file move
#   plus an import. It needs an accessor boundary that keeps the hot read cheap
#   and keeps `Rcu` ownership on one side. Budget it as real work, not a shuffle.
#
#   Target unchanged: 484, all eight functions and both RCU cells together.
#
# settings.zig 400 -> 403. THREE LINES: the `umask` field, its doc line, and the
# `getMode("UMask")` call.
#
#   Newly over the goal, by the same change and for the same reason as main.zig
#   above. There is nothing to split: this file is one parser producing one
#   struct, and the three lines are one option threaded through it exactly as
#   every other option in the file is. Splitting a config parser by option would
#   be the worst available answer.
#
#   Worth stating because it will come up again: this file grows by roughly three
#   lines per configuration option, so it is a slow ratchet with no natural stop.
#   If it reaches ~460 the seam to take is the listener sections -- per-listener
#   `Socket`/`Mode` parsing is a self-contained loop with its own validation and
#   is duplicated in shape across all four daemons, which makes it a candidate
#   for securemilter-lib rather than for a second file here.
#
# 2026-08-05, THE RELOCATED TESTS CAME HOME. sealbuild.zig 412 -> 490,
# settings.zig 403 -> 443, main.zig 648 -> 645, flow.zig 526 unchanged.
#
#   `settings_test.zig`, `sealbuild_test.zig` and `flow_test.zig` are deleted and
#   their tests are back beside the code they cover. Each of those files said in
#   its own header that it existed "so neither the code nor its tests is counted
#   against the other's length" -- which is the relocation loophole stated as a
#   design note, three months after the gate closed it. THE STANDING RULE, now
#   also in the gate's header: comments and tests never count, and a test lives
#   beside the thing it tests. Both halves are needed. Only the first is a metric.
#
#   THIS IS WHAT THE SPLIT WAS HIDING, and it is the reason the raise is honest
#   rather than a regression. A test BODY and the comment above it cost nothing
#   wherever they sit -- `prod_lines` discards both -- so folding the tests back
#   in was free for flow.zig (526, unchanged, still exactly on its ceiling) and
#   free for securedmarc's dmarc.zig (511, unchanged, and it had had NO tests of
#   its own left at all). What is NOT free is shared test SUPPORT, because a
#   helper is a top-level declaration wherever it lives:
#
#     sealbuild.zig  +78   `connWith` (a Connection over a real pipe, since
#                          `deinit` closes whatever fd it is handed), `our_id`,
#                          `priorSets`, and the two comment blocks explaining
#                          why the AAR trust rule and the A-20 seal scope are
#                          pinned directly -- the second says plainly that the
#                          ValiMail suite stays 17/17 with the fix reverted, so
#                          the prose is the evidence, not decoration. +1 more
#                          for `const posix`, used at fourteen call sites.
#     settings.zig   +40   `scratchConfig` plus the A-14 snapshot note; the
#                          helper is 29 lines because an `ArcConfig` has 24
#                          fields and the test needs its strings in scratch
#                          memory to prove the snapshot copied them.
#
#   That cost existed before today. The split billed it to two files with no
#   ceiling and no reader, which is exactly the accounting error the gate exists
#   to stop -- so the number going up here is the number becoming true.
#
#   NO OBLIGATION TO UNDO, and deliberately no attempt to dodge it. Moving the
#   helpers to a third file to keep these two numbers down is the same trade
#   again with an extra step. If sealbuild.zig is ever cut, the seam is the one
#   its own header already names -- AAR content versus signing-input
#   canonicalization -- and the tests go with whichever half they cover.
#
# WRITE THE REASON FOR A CEILING ABOVE THIS LINE.
#
# From here down is regenerated whenever a ceiling moves. A comment
# above that line is preserved; one below it is reported and then lost.
#
# Files currently over the 400-line goal, with the ceiling each
# may not exceed. Ceilings tighten by themselves as files shrink; a
# file that reaches the goal drops off this list entirely.
#
# Adding an entry by hand is how you knowingly accept a long file:
#   zig build lint -- --update
# An empty list below means every file meets the goal.
420 src/arc.zig
628 src/chain.zig
526 src/flow.zig
636 src/main.zig
490 src/sealbuild.zig
443 src/settings.zig
