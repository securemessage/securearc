# Files currently over the 400-line goal, with the ceiling each
# may not exceed. Ceilings tighten by themselves as files shrink; a
# file that reaches the goal drops off this list entirely.
#
# Adding an entry by hand is how you knowingly accept a long file:
#   zig build lint -- --update
# An empty list below means every file meets the goal.
# src/arc.zig 418 -> 528 and src/chain.zig 769 -> 891, for the ValiMail ARC
#   conformance fixes (171/171). Most of both rises is tests: roughly 110 lines in
#   arc.zig and 82 in chain.zig, against about 40 lines of new production code
#   between them. The gate excludes *_test.zig files, so moving them would clear
#   this outright -- but chain.zig's tests exercise isSupportedAlgorithm,
#   signsArcSeal, buildAmsSigningInput and the amsSetWith helpers, and arc.zig's
#   exercise parseInstance, all private. Publishing internals to satisfy a line
#   count is the worse trade, the same conclusion reached for securedmarc's
#   treewalk.zig. securedmarc's dmarc.zig *was* extracted, because every symbol
#   its tests touch was already public.
#
#   chain.zig is now the largest file in this repo and is the obvious next R1
#   target: R1 took main.zig from 1698 to 603 and stopped there.
528 src/arc.zig
891 src/chain.zig
419 src/flow.zig
603 src/main.zig
